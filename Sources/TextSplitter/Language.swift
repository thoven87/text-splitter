/// Programming and markup languages with built-in separator sets.
public enum Language: String, Sendable, CaseIterable {
    case c, cpp, go, java, kotlin
    case js, ts, php, proto
    case python, r, rst, ruby, rust
    case scala, swift
    case markdown, latex, html
    case sol, csharp, cobol
    case lua, perl, haskell, elixir
    case powershell, visualBasic6
}
